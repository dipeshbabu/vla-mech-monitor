env:
  task_suite_name: libero_object
  num_trials_per_task: 1
  seed: 42
  num_steps_wait: 10

model:
  family: openvla
  name: openvla-7b
  device: cuda
  dtype: bfloat16

intervention:
  enabled: false

monitor:
  enabled: false

logging:
  root_dir: ./runs
  save_video: false
  save_actions: true
  verbose: true
